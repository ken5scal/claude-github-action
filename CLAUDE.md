# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is currently an empty project directory with no codebase to analyze. When code is added to this project, this file should be updated with:

1. Build, lint, and test commands specific to the project
2. High-level architecture and structure information
3. Any project-specific conventions or patterns

## Current Configuration

The project has a Claude settings file at `.claude/settings.local.json` that restricts permissions to only allow `ls` commands via Bash.